# 16 — Captured Memories

Atomic memories Vision captured automatically that did not belong to one of the
named categories. Each note carries `memory: true` and decays according to its
`retention_class`.

This folder is populated by use — it is normal for it to be empty.

See [[Memory/99 - Memory Rules|99 — Memory Rules]] for the decay policy.
